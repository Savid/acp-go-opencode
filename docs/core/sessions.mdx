# Sessions

Each ACP session owns one OpenCode server process and four isolated XDG
directories under the configured home parent.

Lifecycle support:

- `session/new`
- `session/load`
- `session/resume`
- `session/list`
- `session/close`
- `session/delete`
- `_opencode/session/fork`

`session/load` replays messages from OpenCode REST. `session/resume` restores
native state and returns without replay.
