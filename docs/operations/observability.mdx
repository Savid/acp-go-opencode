# Observability

Use `WithLogger` for adapter diagnostics. The CLI `-debug` flag writes
structured logs to stderr.

OpenTelemetry provider options are part of the common public API, matching the
other ACP sibling packages.
