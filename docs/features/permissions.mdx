# Permissions

OpenCode permission v2 events are mapped to ACP `session/request_permission`.
The adapter replies to OpenCode through the v2 permission reply route.

On cancellation, pending native permission requests are rejected with a
cancelled explanation and the ACP prompt returns `cancelled`.
