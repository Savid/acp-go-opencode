# Updates

The adapter emits ACP `session/update` notifications for:

- user and agent message chunks
- thought chunks
- tool calls
- plan updates
- config option updates
- usage updates

Usage metadata may include `_meta.opencode.messageId`.
